{% extends 'base.html.twig' %}

{% block title %}E-mail envoyé{% endblock %}

{% block body %}
    <div class="container">
        <h1>Vérifiez votre boîte mail</h1>
        <p>
            Si un compte correspond à cette adresse, un e-mail contenant un lien de réinitialisation vient d'être envoyé.
            Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
        </p>
        <p>Si vous ne recevez rien, vérifiez vos spams ou <a href="{{ path('app_forgot_password_request') }}">réessayez</a>.</p>
        <p><a href="{{ path('app_login') }}">← Retour à la connexion</a></p>
    </div>
{% endblock %}
